# STATUS

<!--
Loop control file — see LOOP.md "Control & handoff files" for the full
control-word semantics. Control lines are written at the START of a line,
one per line, optionally followed by ": reason". This comment block must
never contain a control word at line start, or gate greps would self-trip.
-->
